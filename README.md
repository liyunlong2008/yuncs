# yuncs — OKX ETH-USDT-SWAP 永续量化机器人

> 开发/维护前先读 [AGENTS.md](AGENTS.md)——项目硬约束、已验证的 OKX 数据、踩坑记录都固化在里面。

单交易所（OKX）、单交易对（ETH-USDT-SWAP）、小资金（≤20U）挑战赛玩法量化机器人。
**无胜利点**：核心是持续正确操作、把权益做大；一条随盈利加深自动收紧的**动态出局线**
（起步峰值回撤 30% 出局 → 约 1.5 倍后最多回吐 10% 锁利，线性过渡无悬崖）在反转时保护利润，
出局线触发即结束本轮并**进程内自动开新一轮**（纸盘重置初始资金、实盘从当前真实余额复合）。

内置策略：**ma_macd**（VC_kxs/财道先生"两情相悦体系"机械版：15m MA5/MA10+MACD 双交叉
× 1H 生命线 × 四位置 × 136 分仓 × 锁利离场，含可选背离过滤/日线形态守卫）。
**2026-09-06 清理：其余策略族（rsi_revert/donchian/trend_ema/boll/动量/混合等）已删除**——
修复回测"忽略 Signal(close)"潜伏 bug 后，其实盘连续语义下全部实证为负（证据见下方研究史）。

同一套策略代码跑 **回测 / 纸盘 / 实盘** 三种模式，核心计算（强平价、资金费、费用、仓位换算）严格按 OKX 官方口径。

## 特性

- 纸盘 = 真实行情 + 本地撮合（按盘口深度成交 + 滑点 + 手续费 + 资金费 + 强平保护），**绝不影响真实账户**
- 实盘 = 真实下单，持仓/余额/强平价**一律以交易所返回为准**
- 行情源：WebSocket 优先，8 秒无数据自动降级 REST 轮询（本地代理环境可用）
- 玩法适配回测：按轮次逐 K 线回放 + 资金费结算 + 强平检查 + 动态出局线，
  报告输出**每轮结束倍数分布**（锁利轮/止损轮占比、平均/最高倍数）
- FastAPI + 单页 Web 看板：实时权益曲线、当前轮倍数/出局线、持仓（USDT 口径：可买/成本/预估成本价）、成交明细
- 配置全 TOML（无 env 文件），密钥独立 `secrets.toml`（gitignore）；`initial_balance=0` 实盘自动用实际余额

## 技术栈

Python ≥3.12 · uv · ccxt/ccxt.pro · FastAPI · loguru · aiosqlite · pydantic

## 快速开始

```bash
uv sync                       # 安装依赖
cp config.example.toml config.toml
cp secrets.example.toml secrets.toml   # 纸盘可不填 key；实盘必须填
uv run pytest                 # 跑单测
uv run python -m app.run --mode paper  # 纸盘跑起来（含看板 http://127.0.0.1:8000）
```

开发机（Windows）访问 OKX 需要代理：`config.toml` 里 `proxy = "http://127.0.0.1:10808"`；
VPS 直连留空，`feed = "auto"` 会自动用 WebSocket。

## 回测

```bash
# 下载 1m K 线（CSV 缓存到 data/，第二次跑不再下载）+ 资金费历史，回放挑战
uv run python -m app.backtest_cli --start 2026-08-25 --end 2026-09-01
uv run python -m app.backtest_cli --start 2026-08-01 --end 2026-08-31 --timeframe 5m --force
```

回测与纸盘/实盘共用策略、挑战引擎、撮合模型与 OKX 计算方法，结论可直接迁移到实盘。

## 实盘

```bash
# 1) secrets.toml 填 OKX API key（需开交易权限，建议仅子账户 + 单独提币白名单）
# 2) config.toml: mode = "live"，建议先在 paper 下跑通几天
# 3) 启动（VPS 用 systemd，见 deploy/）
uv run python -m app.run --mode live
```

安全设计：实盘模式无 key 拒绝启动；启动预检（余额/规格/杠杆）；每 10s 从交易所对账；
`POST /api/kill` 或 Ctrl+C 平仓结算；挑战回撤出局自动全平停止。

## 目录

```
app/
  config.py       TOML 配置加载校验
  okx_feed.py     行情订阅（WS/REST 自动切换）+ 合约规格/费率拉取
  okx_math.py     OKX 官方计算方法（强平价/资金费/成本/换算）
  fills.py        撮合与滑点模型（纸盘+回测共用）
  wallet.py       纸盘钱包（独立记账）
  broker.py       paper/live 统一接口，持仓以交易所为准
  strategy.py     策略框架 + ma_macd（唯一内置策略）
  challenge.py    轮次引擎（动态出局线：容忍率线性收紧，无胜利点）
  engine.py       主循环（bar 驱动 + 资金费 + 强平保护 + 状态广播）
  backtest.py     玩法适配回测
  store.py        SQLite（挑战轮次/成交/权益曲线）
  api.py           REST + WebSocket + 静态看板
tests/            okx 计算方法/挑战规则/撮合/回测确定性
deploy/           VPS 部署（systemd + caddy）
```

## 部署（VPS Ubuntu）

见 `deploy/README.md`：`uv sync --frozen` + systemd 常驻 + caddy 反代看板（Basic Auth，对外端口 8765）。

## 策略研究小结（2026-09，真实 OKX 数据）

**2026-09-06 状态：唯一保留 ma_macd；默认与配置均指向它（仍未过 1.0 盈利红线，
只做纸盘前向验证，不投实盘真钱）。**下方为完整研究史（旧策略为何被删的证据）。

系统性筛过 8+ 策略族（趋势突破/EMA 趋势/动量/布林/RSI 回归/随机RSI/SuperTrend/震荡趋势双模混合），
验证协议 = 15m 实盘连续语义 + 5x 杠杆 + 训练/样本外多窗口拆分。收敛结论：

- **唯一跨全部独立窗口为正：`rsi_revert`（RSI(2)+SMA200 趋势过滤，lo10/5x/15m）**
  （三窗口 1.11/1.03/1.66x + 此前 3 年逐年 2.29/2.24/1.15x 全正；胜率仅 6~9%，靠少数大反弹盈利）
- boll_revert 次之（2/3 窗口正）；ts_momentum 仅在趋势期窗口正；donchian/EMA/混合全负
- **5m 周期对此类策略噪声过大，一律用 15m**
- **20x 杠杆是均值回归的死亡区（盈亏比倒挂），5x 才成立；10x 亦是悬崖**

**当前状态**：ma_macd 待纸盘前向验证（VPS 1~2 周），通过前不投入实盘。
配置示例：`config.example.toml`（15m / ma_macd / 5x / 固定 5U 保证金）。

### 补充研究（2026-09-06）：136 分批体系 ma_macd + 20U 玩法口径复核

按 VC_kxs 体系（MA5/10+MACD 两情相悦 × 1H 生命线 × 四位置 × 136 分仓 × 锁利离场）实现机械版 `ma_macd`，
并在 20U 玩法口径（15m、5x、固定 5U 与 25% 缩放保证金、`compounding=True`）下与 rsi_revert 同窗对照：

| 窗口@15m | ma_macd 默认 | 仅顺线 t3/t4 | confirm=any | 关放量过滤 | rsi_revert 同口径 |
|---|---|---|---|---|---|
| 3年（固定5U） | 0.998x | 0.993x | 0.980x | 0.956x | **0.015x** |
| 近1年（固定5U） | 0.997x | 0.999x | 0.966x | 0.976x | **0.015x** |
| 3年（25%缩放） | 0.995x | 0.993x | — | — | **0.062x** |
| 近1年（25%缩放） | 0.997x | 0.999x | — | — | **0.095x** |

- ma_macd 各变体稳定在 ~0.95~1.00x（含费后微亏、接近打平），但**无一窗口突破 1.0 验收红线**；
  且 3 年仅 33 笔（约 1~3 笔/月），统计上无法与 0 区分——继续调参只是过拟合这几十笔样本，故不切换默认。
- **重要发现：rsi_revert 在 20U + 固定 5U/25% 缩放口径下同样覆灭（0.015~0.095x）**——早年
  "唯一全正"结论依赖当时的小账户口径与窗口段；当前玩法口径下**所有候选策略均不成立**。
  这指向瓶颈在资金层而非信号层（单笔保证金占 25% + 动态出局线 30%/10% 的复利损耗组合），
  是下一步研究的重点，不是"再换一个策略"能解决。
- 结论（当时的红线决定，此后 rsi_revert 已删除）：ma_macd 为唯一内置与默认策略，
  机械规则与参数见 strategy.py `MaMacd` 文档串。**仍未过盈利红线，不要投入实盘真钱**。

### 2026-09-06 追加：回测 close 修复推翻 rsi_revert 旧结论；资金层死区机制

- **背景**：本重构修复了一个潜伏 bug——回测里策略的 `Signal("close")`（如 RsiRevert 的 EMA5 反弹到位、
  ma_macd 的高周期反向清仓）**从未被执行**，只有引擎/纸盘会平。修复后回测与实盘语义一致，
  旧研究（含"rsi_revert 唯一跨窗口全正"）全部基于这个不完整闭环，结论作废。
- **rsi_revert 在修复后的真实语义下全负**（3y/近1y compounding，20U、5x）：固定 2/3/5U 保证金 0.013~0.026x；
  10%/15% 缩放最好也只有 0.107~0.376x；容忍率 20/10 与 40/15 均救不回（交易数百~两千笔，
  高胜率低赔率结构被尾部亏损+复利磨平）。**该策略不再具备"候选"资格。**
- **ma_macd（136 分批）存在最小下单死区**：20U 账户下保证金 <5U（名义 <25U@2500）时，
  136 首层 10% 不足最小 0.01 张 → 全程 0 交易（回测各档均为精确 1.000x）。唯一可交易档
  （5U 固定=25%）复合 0.997~0.998x，接近打平、无强平、无出局线触发。
- 资金层结论修正：小账户 × 分批首层 × 0.01 张最小下单构成死区下限；能交易的档位下，
  目前所有候选都非正期望——**玩法层（20U 挑战赛语义）在真实摩擦下没有已验证的正期望策略**，
  继续的方向应是更大止损/更长持有周期/更高质量信号（而非参数微调），且必须过本文件的验证协议。

### 2026-09-06 追加：杠杆全级别敏感性（≤50x，compounding，20U/固定5U）

| 杠杆 | ma_macd 3年 | ma_macd 近1年 | 3年强平 | 备注 |
|---|---|---|---|---|
| **5x** | **0.998x** | **0.997x** | 0 | 最优档（默认） |
| 10x | 0.971x | 0.989x | 0 | |
| 15x | 0.952x | 0.981x | 0 | |
| 20x | 0.932x | 0.972x | 0 | |
| 30x | 0.872x | 0.960x | 2 | |
| 40x | 0.820x | 0.940x | 5 | |
| 50x | 0.705x | 0.927x | 7 | 触发 1 轮出局线 |

- **≤50x 全级别单调变差，不存在"某个高杠杆档位转正"**：136+小目标+锁利结构把每笔收益压到
  手续费边缘，高杠杆只放大费用与尾部亏损；30x 起出现强平、50x 出局线触发。
- 已删除的旧策略（rsi_revert 等）任何杠杆档位同样全负（补测 15x/30x/40x = 0.005/0.002/0.002x）——
  删除决定由全部证据支持。
- 提高保证金占比（5x 下 25%→50%）同样单调变差（3年 0.995→0.968x）——信号层边际略负，资金层无解。
### 2026-09-06 追加：v3/v4 —— 正期望 × 频率边界（近 60 变体扫描后的事实）

- **v3（参数面）**：频率可用 confirm=any 解锁到 104~148 笔/年（接近日频），但全部负期望
  （0.93~0.97x）——频率与收益严格反向；参数调不出"既高频又为正"。
- **v4（结构：日线趋势偏置 d1_bias_ma）**：加"多头日只做多/空头日只做空"后单笔质量大增
  （胜率 50%→60~80%），近 1 年出现 1.000x 行；但高质量与二次确认叠加后频率塌到 4~7 笔/年。
- **逐年稳健性（候选 J1=bias10、J12=bias10+骑乘）**：三年逐年全部 ≥1.000x（每年仅 3~7 笔、
  累计复合约 +0.1~0.4%）；而同一偏置下 30~60 笔/年档逐年仍 <1.0（-0.1%~-2%/年）。
- **边界结论**：当前机械工具箱内（单对 15m 双交叉 + 摩擦成本），"财道式日频"与正期望互斥；
  正期望只存在于月频级别的超精选信号（且幅度小到几乎无法与 0 区分）。要同时拿到日频与正期望，
  需要人工图面级特征（精确支撑压力/背离真假判别）或多标的轮动——超出当前机械可测范围，如实记录。
- 新增参数：`d1_bias_ma`（0=关；>0 日线收盘 vs D1 MA(N) 方向门，多头日仅 t1/t3、空头日仅 t2/t4，
  持仓中方向翻转即离场）；引擎历史窗口升至 3000 根以支持日线 MA20。

### 2026-09-06 追加：v5 —— 1H 结构位引擎（财道图面支撑/压力的第一版机械化）

- 实现：1H 摆动点(±2根)聚类成水平位、触碰≥2 次为有效；T1/T2 入场须贴近有效支撑/压力，
  目标位改用最近结构位；参数 `struct_levels`（默认关）+ 单测。
- 近1年结果：结构位单独用反而更差（0.995x/胜率33%——机械聚类位质量不足）；
  与日线偏置合并后回到"低频 1.000x 平台"（3~5 笔/年、胜率 67~75%），
  **未能打开频率-收益空间**。至此 5 轮 >80 变体一致结论：机械可测空间内
  "正期望"与"日频"不可兼得；要突破需换信号级别（1H 交叉作方向/持仓周期）或
  多标的轮动（与 AGENTS 单交易对硬约束冲突，需用户决策）或人工特征深度工程化。

### 2026-09-06 追加：v6 —— macd_1h（1H 交叉定趋势/15m 执行/陪跑离场）

- 实现：新策略类 `macd_1h`（已注册可实验启用）：每闭合 1H 桶判定 MA5/MA10+MACD 两情相悦，
  金叉开多/死叉清多（镜像），保本+1H 高点 chandelier 追踪；单测覆盖开仓与反手平仓。
- 双窗结果（compounding，20U/5x/固定5U）：**频率 128~149 笔/年（≈2.5 天一笔，最接近作者节奏），
  但 3年 0.59x、近1年 0.64~0.83x 全面大幅为负**——1H 双交叉趋势跟随在 taker 摩擦下持续失血
  （呼应研究史：EMA/MACD 交叉族在任何周期都不扛摩擦）。
- **六轮 >90 变体后的完整边界**：
  | 结构族 | 频率 | 复合 | 结论 |
  |---|---|---|---|
  | 15m 均值回归/四位置参数面 | 30~150/年 | 0.93~0.99 | 高频必负 |
  | ma_macd+日线偏置超精选 | 3~7/年 | 三年逐年 ≥1.000 | 唯一"不亏"，月频 |
  | 1H 交叉趋势(macd_1h) | 130~150/年 | 0.59~0.83 | 高频大幅为负 |
  - 机械空间内"正期望"与"接近日频"依旧不可兼得；正期望候选目前只有超精选族（见 v4/v5 表）。

### 2026-09-06 追加：ma_macd v2（放量口径/背离/日线守卫，帖子最新口径对齐实验）

按作者 2026-09 帖子与 B 站教学实现三项：持仓放量规则修正（帖子称日内放量多为洗盘）、
MACD 背离入场过滤（B站"底背离底部反弹/顶背离高位回调"）、日线前高多次试探+新高针守卫
（帖子"多单都走，布局回调空"）。compounding 复核（20U/5x/固定5U）：

| 配置@15m | 3年复合 | 近1年复合 | 备注 |
|---|---|---|---|
| v2 默认（放量止盈关） | 0.976x | 0.995x | 变差 |
| **放量止盈开（默认）** | **0.998x** | **0.997x** | 双窗最优 |
| +背离过滤 div_filter | 1.000x（0 笔） | 1.000x（0 笔） | 15m 背离把信号全滤光，默认关 |
| +日线守卫 d1_enable | 0.976x | 0.995x | 双窗无增益，默认关 |

- **回测证据推翻"作者最新直觉"的机械搬运**：作者说"日内放量不是真跌、别被搞破防"，
  但其依赖人工区分洗盘与真转势；机械版保留"放量大反向止盈式离场"双窗都更优
  （0.998/0.997 vs 0.976/0.995）——默认保持开启，`vol_exit_opposite=False` 可贴近作者表述。
- 背离（15m）与日线守卫 v1 判定当前不可用/无增益，均为默认关的研究参数；
  背离若要做需移师 1H 级别另行研究（作者实盘用的判图级背离无法直接等价到 15m 机械规则）。

## 风险提示（务必阅读）

**当前内置策略（ma_macd）在真实资金语义下尚未过正期望验证（3y/近1y 约 0.997~0.998x），
本仓库定位是玩法/风控引擎与实验台，不是可盈利策略。**

- 回测语义警告：**"纸盘重置"回测（每周期重置初始资金）严重高估真实结果**。纸盘/VPS 运行与真实账户都是连续资金，
  周期亏损会复利叠加。**任何"回测盈利"的结论必须用实盘连续语义复核**（`Backtest(compounding=True)`）。
- 基于真实 OKX 数据的验证（2026-09 完成）：1年@5m、3年@15m，实盘连续语义下当前 donchian 30/15@20x
  无论有无 ADX 趋势过滤、固定或缩放保证金，均把 20U 磨到接近零（复合 0.01~0.24x，取决于是否触发最小下单停摆）。
  纸盘重置语义下 3 年正周期率 36%、单周期峰值 2.6x，但无法弥补多数亏损周期的复利损耗。
- 结论：**不要投入实盘真钱**，除非策略通过 `compounding=True` 的实盘语义回测验证为正期望
  （验证脚本：`scripts/bt_ma_macd.py`）。
- 小资金高杠杆玩法本身风险极高；本机器人带强平保护、出局线与进程重启恢复，但**机械正确 ≠ 策略盈利**。
  资金损失风险自负。
